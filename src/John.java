/**
 * This is the entry point and the primary loop for the program. If anything interesting happens, it happens here.
 * @author cnewb
 *
 */

/**
 * To use classes, you'll have to import them like so:
 * import Support.Coordinate;
 * I would say just import the whole "package", but it's frowned upon
 * and Java doesn't do namespaces, so importing EACH needed class will be required.
 * (For testing it'll be fine importing everything)
 */

public class John {

	public static void main(String[] args) {
		System.out.print("PAPA John! Get it?");
	}

}
